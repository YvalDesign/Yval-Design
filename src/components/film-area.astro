---
import Layout from "../layouts/main.astro";
import { Image } from "astro:assets";
import cam from "../assets/kamera.png";
import Mivim from "../assets/Imagefilm.mp4";
import Eule from "../assets/Eule.mp4";
import Animiv from "../assets/Animiv.mp4";
---

<section class="web">
  <div class="webhero">
    <h2 class="webheadline">
      KEINER CHECKTS? <br />
      <span class="h2-span"> Wir erklären es für dich!</span>
    </h2>

    <Image
      src={cam}
      alt="Offener Laptop mit einer offenen Website und einer Raketengrafik im Hintergrund"
      class="hero"
      loading="lazy"
      quality="{80}"
      decoding="async"
      placeholder="blur"
      format="avif"
      widths={[800, 1600, 3000]}
    />

    <p class="webtext">
      Frustriert, weil niemand wirklich sieht, wie gut dein Produkt ist - oder
      wie stark die Zusammenarbeit mit dir eigentlich wäre? Und du bekommst es
      einfach nicht in Worte gefasst?
    </p>
  </div>
  <div class="websites">
    <h3>UNSERE IMAGEFILME</h3>
    <div class="ex">
      <video controls muted preload="metadata" playsinline class="videoex">
        <source src={Mivim} type="video/mp4" />
        <source src={Mivim} type="video/webm" />
        Dein Browser unterstützt dieses Videoformat nicht.
      </video>
    </div>
    <h3>UNSERE ANIMATIONEN</h3>
    <div class="ex">
      <video controls muted preload="metadata" playsinline class="videoex">
        <source src={Eule} type="video/mp4" />
        <source src={Eule} type="video/webm" />
        Dein Browser unterstützt dieses Videoformat nicht.
      </video>
      <video controls muted preload="metadata" playsinline class="videoex">
        <source src={Animiv} type="video/mp4" />
        <source src={Animiv} type="video/webm" />
        Dein Browser unterstützt dieses Videoformat nicht.
      </video>
      <div class="spacer"></div>
    </div>
    <button>LASS ES UNS IHNEN ZEIGEN!</button>
  </div>
</section>

<style>
  h2 {
    color: var(--primary);
    font-size: 8vw;
    font-weight: var(--weight-bold);
    font-family: var(--gothic-wide);

    text-align: center;
    line-height: 0.6;
  }
  .h2-span {
    color: white;
    font-family: var(--spills);
    font-weight: var(--weight-normal);
    font-size: 8.5vw;
  }

  .web {
    margin-block-start: 100px;
    margin-block-end: 10%;
  }

  p {
    font-size: 1rem;
    line-height: 130%;
    font-family: var(--gothic-wide);
    font-weight: var(--weight-semibold);
    color: white;

    text-align: center;
    margin-inline: auto;
  }

  .hero {
    width: 160%;
    height: auto;
    margin-left: 7%;
    max-width: 1300px;
  }

  @media (min-width: 701px) {
    h2 {
      font-size: 7vw;
    }

    .h2-span {
      font-size: 7.5vw;
    }

    p {
      font-size: 1.75rem;
    }
    .hero {
      width: 120%;
      margin-top: 2rem;
    }
  }

  @media (min-width: 1200px) {
    h2 {
      font-size: 6vw;
    }
    .h2-span {
      font-size: 6.5vw;
    }
    p {
      font-size: 2rem;
    }
  }

  @media (min-width: 1440px) {
    .hero {
      margin-top: 5rem;
    }
  }

  .webhero {
    position: relative;
    display: flex;
    flex-direction: column; /* Headline oben, Bild unten */
    align-items: center; /* Abstand proportional zur View Width */
    overflow-x: clip;
    overflow-y: visible;
  }

  .webheadline {
    position: absolute;
  }

  .webtext {
    position: absolute;
    inset-block-end: -1.5rem;
    max-width: 600%;
  }
  @media (min-width: 701px) {
    .webtext {
      inset-block-end: -7rem;
    }
  }

  @media (min-width: 1000px) {
    .webtext {
      inset-block-end: -1rem;
    }
  }
  .websites {
    max-width: 1200px;
    margin-block-start: 80px;
    margin-inline: auto;
    display: flex;
    flex-direction: column;
    align-items: center; /* Bild mittig */
    justify-content: center;
    padding-inline: 1rem;
  }
  .ex {
    display: flex;
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    gap: 1rem;
    padding-bottom: 1rem;
    scroll-behavior: smooth;
    scrollbar-width: thin;
  }

  .videoex {
    flex: 0 0 auto;
    scroll-snap-align: center;

    border-radius: 20px;
    width: 95%;
    height: auto;
  }

  .spacer {
    flex: 0 0 auto;
    scroll-snap-align: center;
    max-width: 65%;
    height: auto;
  }

  h3 {
    color: white;
    font-family: var(--gothic-xwide);
    font-weight: 600;
    font-size: 4vw;
    align-self: flex-start;
    margin-top: 3rem;
  }

  @media (min-width: 701px) {
    h3 {
      font-size: 2rem;
    }
  }

  button {
    background: linear-gradient(
      to right,
      var(--primary),
      var(--secondary),
      var(--primary)
    );
    background-size: 200%;
    background-position: center;
    border: none;
    border-radius: 50px;
    color: white;

    font-family: var(--gothic-wide);
    font-weight: 800;
    display: block;
    margin-inline: auto;

    transform: scale(1);
    transition: transform 150ms ease-in-out;
    cursor: pointer;
  }

  @media (max-width: 700px) {
    button {
      font-size: 4vw;
      padding: 20px 40px;
      margin-top: 2rem;
    }
  }

  @media (min-width: 701px) {
    button {
      font-size: 3vw;
      padding: 25px 35px;
      margin-top: 4rem;
    }
  }

  @media (min-width: 1000px) {
    button {
      font-size: 2vw;
      padding: 30px 45px;
    }
  }

  button:hover {
    transform: scale(1.05);
    transition: all 150ms ease-in-out;
  }

  button:active {
    transform: scale(0.97);
  }
</style>
